/**
Given an array of integers nums, write a method that returns the "pivot"
index of this array. We define the pivot index as the index wherer the 
sum of the numbers to the left of the index is equals to the sum of the
right.If no such index exists, we should return -1. If there are multiple
 pivot indexes, you should return the left-most pivot;
**/
/**
Input:
nums = [1, 2, 3]
Output: -1

Input: 
nums = [1, 7, 3, 6, 5, 6]
Output: 3
 **/
#include <iostream>
#include <vector>

int pivotIndex(std::vector<int>& nums)
{
  
  return -1;
}

int main()
{
  std::vector<int> nums = {1, 7, 3, 6, 5, 6};
  
  return 0;
}
